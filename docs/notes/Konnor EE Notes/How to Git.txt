Get to Directory:
1. Open command prompt
2. > cd C:\Users\wordo\Desktop\AdamPC

Push to repository
1. > git add *
2. > git commit -m "COMMENT"
3. > git push

Other Git commands
> git branch
  Shows current branch
> git switch BRANCH_NAME
  Switches to a branch
> git branch -a
  Lists branches
> git help
  Shows available commands within git